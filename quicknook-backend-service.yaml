apiVersion: v1
kind: Service
metadata:
  name: quicknook-backend
  namespace: quicknook
spec:
  selector:
    app: quicknook-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
